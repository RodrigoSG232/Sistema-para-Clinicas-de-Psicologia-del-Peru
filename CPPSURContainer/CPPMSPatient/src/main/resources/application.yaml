spring:
  application:
    name: cpp-patient-service
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

info:
  app:
    name: CPP Patient Service
    description: Gestion independiente de pacientes e historias clinicas
